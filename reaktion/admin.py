from django.contrib import admin
from reaktion import models

# Register your models here.
admin.site.register(models.Flow)
admin.site.register(models.Workspace)
admin.site.register(models.ReactiveTemplate)
